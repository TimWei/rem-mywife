    // $(document).ready(function(){
	SCENE_MNR.set_ui('control_view');
    function init(){
        obj = new_div('bill_board');
        obj.set_rect(700,450);
        obj.move_to(650, 300);

	}

	init();